# 项目协作规则

## 工作流程

1. 任何功能开发、重构或大范围代码调整，必须先做计划，不直接改业务代码。
2. 计划需要落盘到 `docs/plans/` 目录，文件名使用清晰的日期和主题，例如 `docs/plans/2026-05-03-data-platform-mvp.md`。
3. 计划文件至少包含目标、范围、实现步骤、数据/API 变化、验证方式和风险点。
4. 计划整理完成后，先交给用户确认。
5. 用户明确确认计划无误后，再开始代码开发。
6. 如果开发过程中发现计划需要变更，先更新计划文件并说明变化，再继续开发。
7. 每个任务完成后，需要做详尽测试；涉及前端时，需要从页面各处验证主要交互，包括按钮、上传、下载、增删改查等真实流程。
8. 每个任务完成并验证后，需要提交代码并推送到远端仓库。

## 代码原则

1. 优先保持实现简单、可运行、可验证。
2. 优先沿用项目已有技术栈和目录结构，不随意引入新框架或重依赖。
3. 涉及数据存储、上传、下载、权限、删除等能力时，需要明确边界和失败处理。
4. 前端页面以真实可用为目标，避免只做展示页。
5. 每次改动后尽量运行可用的格式化、编译或测试命令，并在交付时说明验证结果。

## 计划模板

```markdown
# 计划：<主题>

## 目标

## 范围

## 不做什么

## 实现步骤

## 数据与 API 变化

## 验证方式

## 风险与待确认
```
